const Header = () => {
  return (
    <span
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      HEADER COMPONENT
    </span>
  );
};

export default Header;
